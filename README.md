# ShiroDev clone

Site estatico com tres paginas:

- `index.html`
- `projetos.html`
- `setup.html`

Pode abrir o `index.html` direto no navegador. Se usar Live Server no VS Code, os links tambem funcionam normalmente.
